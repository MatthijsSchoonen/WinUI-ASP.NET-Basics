<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUI_Basics.Views.CartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_Basics.Views"
    xmlns:localData="using:WinUI_Basics.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#FFFAE4">
        <StackPanel Margin="20">
            <TextBlock Text="Your Cart" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <ListView x:Name="PizzaListView" >
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="localData:Pizza">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}" FontSize="18" Width="200"/>
                            <TextBlock  FontSize="18" Width="100">€ <Run Text="{x:Bind Price}"  FontSize="18"></Run></TextBlock>
                            <Button Content="Remove" Click="RemovePizza_Click" Tag="{x:Bind}" Margin="10,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="300" Margin="0 20">
            <TextBlock Text="Total:" FontSize="18" FontWeight="Bold">€ <Run x:Name="TotalPriceTextBlock" FontSize="18"></Run></TextBlock>
            <Button Content="Order" Click="OrderButton_Click" HorizontalAlignment="Right" Width="100"/>
        </StackPanel>
    </Grid>
</Page>
